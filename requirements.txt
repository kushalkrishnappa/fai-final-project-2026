# LendWise — loan sanction amount prediction
# Verified against Python 3.14.4 (pyenv env "pytorch") on 2026-08-06.
#
#   python -m venv .venv && source .venv/bin/activate
#   pip install -r requirements.txt

# --- core ---
numpy>=2.0,<2.5        # numba (via shap) cannot import against numpy >= 2.5
pandas>=2.2
scipy>=1.11

# --- modelling ---
scikit-learn>=1.4      # root_mean_squared_error; max_features='auto' removed in 1.3
torch>=2.2             # neural network. TensorFlow has no wheel for Python 3.14.

# --- interpretability ---
shap>=0.45

# --- plotting ---
matplotlib>=3.8
seaborn>=0.13

# --- notebook ---
jupyterlab>=4.0
ipykernel>=6.29
